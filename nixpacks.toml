[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build -w @wellanalytics/api"]

[start]
cmd = "npm run start -w @wellanalytics/api"
